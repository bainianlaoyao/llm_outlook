import os

# 纯Outlook本地邮件系统配置
# 零网络依赖，企业级安全解决方案

# 配置文件路径（已移除，配置来自 config.config）

# 日志配置
LOG_CONFIG = {
    'level': 'INFO',
    'format': '%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    'file_path': 'logs/auto_mail.log',
    'max_file_size': 10485760,  # 10MB
    'backup_count': 5
}

# 示例配置模板（已移除，使用 config.config 提供配置）
